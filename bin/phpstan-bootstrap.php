<?php
/**
 * PHPStan bootstrap: the plugin constants xenios-kb-bot.php defines at runtime.
 * PHPStan only reads that file, so without this every use is "undefined".
 */

define( 'XENIOS_KB_BOT_VERSION', '0.0.0' );
define( 'XENIOS_KB_BOT_PATH', dirname( __DIR__ ) . '/' );
define( 'XENIOS_KB_BOT_URL', 'https://example.test/wp-content/plugins/xenios-kb-bot/' );
